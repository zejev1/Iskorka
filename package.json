{
  "name": "iskorka",
  "version": "0.1.0-stage1",
  "private": true,
  "description": "Independent autonomous human world; physical F2 transplant, first stage.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node scripts/run-tests.mjs",
    "check": "npm run typecheck && npm test",
    "verify": "npm run standalonecheck && npm run check && npm run build && npm run standalonecheck",
    "standalonecheck": "node scripts/check-standalone-tree.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "esbuild": "0.25.12",
    "typescript": "5.8.3",
    "fake-indexeddb": "6.2.2"
  }
}
