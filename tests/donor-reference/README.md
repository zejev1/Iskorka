# Неперезапущенные исторические тесты F2

Эти 87 исходных файлов сохранены без массовой переписки ожиданий. Они не входят в `npm test`: часть относится к удалённому Cardinal/другим расам, прочие требуют адаптации старых fixtures/API. Не считать их пройденными. Пути импортов соответствуют прежнему расположению донора. Классификация: `validation/donor-test-classification.json`. Актуальные адаптированные физические тесты — в `tests/iskorka/`.
